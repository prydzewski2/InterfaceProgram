import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.termasmart.production',
  appName: 'ProductionApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
